$(function() {
   
   $('.carousel').carousel({
       interval: 3000, // Determina o intervalo entre cada imagem
       // pause: "null", // Não pausa a rolagem se o cursor estiver sobre a imagem
       // wrap: false // Não retorna para a primeira imagem
   }); 
    
});